# .gitignore
# ============================================================================
# Module: Git Ignore Rules
# Description: Repository-wide ignore patterns for generated and local artifacts.
# Purpose: Prevent accidental commits of build outputs and runtime data.
# Dependencies: None
# ============================================================================
#
# ## Overview
# - Tracks build, cache, and runtime artifacts across the workspace.
# - Keeps starter-pack bundles and secrets out of version control.
# - Security posture: Reduces leakage of local data (Docs/security/threat_model.md).
#
# Generated by Cargo
# will have compiled files and executables
debug/
target/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Python bytecode/cache artifacts
__pycache__/
*.pyc

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/
/.VSCodeCounter
/proptest-regressions

# System test artifacts (temporary test outputs)
/system-tests/.tmp/
/system-tests/logs
/artifacts
!/artifacts/
!/artifacts/config/
!/artifacts/config/*.schema.json
/perf-report-test
/logs
/perf-report
/data
/logs
/starter-pack/data/
/starter-pack/logs/
/starter-pack/namespace_changefeed.checkpoint.json
/starter-pack/.secrets/
/starter-pack/docker-images/*
!/starter-pack/docker-images/README.md
.assetcore/
.assetcore_build_id
/.tmp
/Docs/testing/MCP/results/run-2025-12-22-codex/artifacts
/.venv
/.venv-runpack
/.cargo/advisory-db/

# Node.js dependencies
node_modules/
